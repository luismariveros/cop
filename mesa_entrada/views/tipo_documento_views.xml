<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Vista de árbol para Tipos de Documento -->
    <record id="view_tipo_documento_tree" model="ir.ui.view">
        <field name="name">tipo.documento.tree</field>
        <field name="model">tipo.documento</field>
        <field name="arch" type="xml">
            <list string="Tipos de Documento" editable="bottom">
                <field name="nombre"/>
                <field name="codigo"/>
                <field name="descripcion"/>
                <field name="activo"/>
            </list>
        </field>
    </record>

    <!-- Vista de formulario para Tipos de Documento -->
    <record id="view_tipo_documento_form" model="ir.ui.view">
        <field name="name">tipo.documento.form</field>
        <field name="model">tipo.documento</field>
        <field name="arch" type="xml">
            <form string="Tipo de Documento">
                <sheet>
                    <group>
                        <group>
                            <field name="nombre"/>
                            <field name="codigo"/>
                        </group>
                        <group>
                            <field name="activo"/>
                        </group>
                    </group>
                    <group>
                        <field name="descripcion"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Acción de Ventana para Tipos de Documento -->
    <record id="action_tipo_documento" model="ir.actions.act_window">
        <field name="name">Tipos de Documento</field>
        <field name="res_model">tipo.documento</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Crear un nuevo Tipo de Documento
            </p>
        </field>
    </record>
</odoo>