<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Vista de árbol para Providencias -->
    <record id="view_providencia_tree" model="ir.ui.view">
        <field name="name">providencia.tree</field>
        <field name="model">providencia</field>
        <field name="arch" type="xml">
            <list string="Providencias" editable="bottom">
                <field name="nombre"/>
                <field name="codigo"/>
                <field name="descripcion"/>
                <field name="activo"/>
            </list>
        </field>
    </record>

    <!-- Vista de formulario para Providencias -->
    <record id="view_providencia_form" model="ir.ui.view">
        <field name="name">providencia.form</field>
        <field name="model">providencia</field>
        <field name="arch" type="xml">
            <form string="Providencia">
                <sheet>
                    <group>
                        <group>
                            <field name="nombre"/>
                            <field name="codigo"/>
                        </group>
                        <group>
                            <field name="activo"/>
                        </group>
                    </group>
                    <group>
                        <field name="descripcion"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Acción de Ventana para Providencias -->
    <record id="action_providencia" model="ir.actions.act_window">
        <field name="name">Providencias</field>
        <field name="res_model">providencia</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Crear una nueva Providencia
            </p>
        </field>
    </record>
</odoo>